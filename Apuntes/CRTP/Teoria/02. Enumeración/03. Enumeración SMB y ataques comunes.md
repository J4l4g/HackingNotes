## Enumeración SMB
### NETEXEC
##### Enumerar las maquinas del dominio
```shell
nxc smb <ip/mascara>
```
##### Validar credenciales
```shell
nxc smb <ip> -u 'jdoe' -p 'Password123'
```
##### Enumeracion de maquinas validas para la conexion
```shell
nxc smb <ip/mascara> -u 'jdoe' -p 'Password123'
```
##### Enumerar recursos compartidos
```shell
nxc smb <ip> -u 'jdoe' -p 'Password123' --shares
```
##### Enumerar usuarios y grupos
```shell
nxc smb <ip> -u 'jdoe' -p 'Password123' --users
nxc smb <ip> -u 'jdoe' -p 'Password123' --groups
```
##### Volcar *LSA* y *NTDS* si tiene privilegios de administrador de dominio
```shell
nxc smb 192.168.1.1 -u 'jdoe' -p 'Password123' --lsa
nxc smb 192.168.1.1 -u 'jdoe' -p 'Password123' --ntds
```

### SMBMAP
Esta herramienta sirve para enu,erar recursos compartidos *SMB* 
##### Listar recursos compartidos con NullSession
```shell
smbmap -H corp-dc
```
##### Listar recursos compartidos con credenciales validas
```shell
smbmap -H corp-dc -u "devan" -p "P@ssword123!"
```
##### Listar recursos compartidos incluyendo la firma por pantalla
```shell
smbmap -H corp-dc -u "devan" --prompt
```
##### Listar recursos compartidos y leer recursivamente uno de ellos
```shell
smbmap -H corp-dc -u "devan" -p "P@ssword123!" -r "RecursoCompartido"
```
##### Comprobar la versión del Sistema Operativo
```shell
smbmap -H corp-dc -u "devan" --prompt -v
```
##### Comprobar la el estado de la firma
```shell
smbmap -H corp-dc -u "devan" --prompt --signing
```

*SI la firma se encuentra deshabilitada esta se puede utilizar para ataques de **RELAY**  y potencialmente para explotar **EternalBlue***

### SMBCLIENT
